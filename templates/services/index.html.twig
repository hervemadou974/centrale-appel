{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
    <center>
        <h1>L'index des services</h1>
    <center/>
    <table class="table">
        <thead>
            <tr>
                <th>Numéro</th>
                <th>Services</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                <td>{{ service.numero }}</td>
                <td>{{ service.services }}</td>
                <td>
                    <a href="{{ path('app_services_show', {'id': service.id}) }}">Regarder</a>
                    <a href="{{ path('app_services_edit', {'id': service.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('app_parametre') }}"class="btn btn-primary">Retour</a>
    <a href="{{ path('app_services_new') }}"class="btn btn-primary">Nouveau service</a>
{% endblock %}
