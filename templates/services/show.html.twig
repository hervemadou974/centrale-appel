{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
    <h1>Services</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ service.numero }}</td>
            </tr>
            <tr>
                <th>Services</th>
                <td>{{ service.services }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_services_index') }}"class="btn btn-primary">Retour a la liste</a>

    {{ include('services/_delete_form.html.twig') }}
{% endblock %}
