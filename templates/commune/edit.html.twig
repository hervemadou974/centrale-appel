{% extends 'base.html.twig' %}

{% block title %}Modifier la Commune{% endblock %}

{% block body %}
    <h1>Modifier la Commune</h1>

    {{ include('commune/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_commune_index') }}"class="btn btn-primary">Retour à la liste</a>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
