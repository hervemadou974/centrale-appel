{% extends 'base.html.twig' %}

{% block title %}Commune{% endblock %}

{% block body %}
    <h1>Commune</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Code_postale</th>
                <td>{{ commune.codePostale }}</td>
            </tr>
            <tr>
                <th>Nom_commune</th>
                <td>{{ commune.nomCommune }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commune_index') }}"class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('app_commune_edit', {'id': commune.id}) }}"class="btn btn-primary">Modifier</a>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
