{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un utilisateur</h1>
        
        {{ include('utilisateur/_form.html.twig', {'button_label': 'changer'}) }}

        <a href="{{ path('app_utilisateur_index') }}" class="btn btn-primary">Retour à la liste des utilisateurs</a>
    </div>
    
    {{ include('utilisateur/_delete_form.html.twig') }}
</div>
{% endblock %}
