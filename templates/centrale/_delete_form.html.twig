<form method="post" action="{{ path('app_centrale_delete', {'id': centrale.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce formulaire d'appel ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centrale.id) }}">
    <button class="btn btn-primary d-flex justify-content-center align-items-center" style="width: 300px;">Supprimer</button>
</form>
